packages:
  - 'packages/*'
# tsup depends on esbuild's postinstall build step; allowlist it so pnpm
# runs the script without prompting for approval on every install.
onlyBuiltDependencies:
  - esbuild
